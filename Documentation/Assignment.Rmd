```{r, initialization, echo=FALSE}
library(knitr)
# set global options for knitr (if changed, all cashed chunked will be rerun!)
knitr::opts_chunk$set(cache = TRUE, cache.path = "./cache/cache_knitr/assignment/", fig.path = "./cache/fig_knitr/assignment/", global.par = TRUE, fig.align = "center", error = TRUE) # use dpi=100 for small pdfs
docache <- TRUE
```

# Inferring geographic origins {#assignment}

## Preparing the data

