```{r, initialization, echo=FALSE}
library(knitr)
# set global options for knitr (if changed, all cashed chunked will be rerun!)
knitr::opts_chunk$set(cache = TRUE, cache.path = "./cache/cache_knitr/calibration/", fig.path = "./cache/fig_knitr/calibration/", global.par = TRUE, fig.align = "center", error = TRUE) # use dpi=100 for small pdfs
docache <- TRUE
```

# Calibration procedure {#calibration}

## Preparing the data

